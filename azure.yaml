# yaml-language-server: $schema=https://raw.githubusercontent.com/Azure/azure-dev/main/schemas/v1.0/azure.yaml.json

name: remote-mcp-webapp-dotnet
metadata:
  template: remote-mcp-webapp-dotnet
services:
  web:
    project: src/McpServer.ApiKey/
    language: dotnet
    host: appservice
